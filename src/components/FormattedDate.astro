---
interface Props {
	date: Date;
    locale?: string;
}

const { date, locale = 'en-US' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(locale, {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
